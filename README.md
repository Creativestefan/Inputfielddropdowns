
  # Input field with dropdowns

  This is a code bundle for Input field with dropdowns. The original project is available at https://www.figma.com/design/bng13izHwFUcZNFIhHHAIt/Input-field-with-dropdowns.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  